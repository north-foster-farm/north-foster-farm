{{- /* Staging and deploy previews build as production too; Netlify's
  CONTEXT is what tells them apart (see staging-toolbar.html). */ -}}
{{- $context := getenv "CONTEXT" -}}
User-agent: *
{{- if and hugo.IsProduction (or (not $context) (eq $context "production")) }}
Disallow:
{{- else }}
Disallow: /
{{- end }}
Sitemap: {{ "sitemap.xml" | absURL }}
